# Some shells / tools export SOURCE_DATE_EPOCH= (empty). Clang then errors:
# "environment variable 'SOURCE_DATE_EPOCH' ('') must be a non-negative decimal integer".
# `force` overrides an empty or inherited value so builds stay reproducible (epoch 0).
[env]
SOURCE_DATE_EPOCH = { value = "0", force = true }
